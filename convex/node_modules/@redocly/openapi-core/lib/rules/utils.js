import { default as levenshtein } from 'js-levenshtein';
import { isRef, Location } from '../ref-utils.js';
import { getOwn } from '../utils/get-own.js';
import { isDefined } from '../utils/is-defined.js';
import { isNotEmptyArray } from '../utils/is-not-empty-array.js';
import { isPlainObject } from '../utils/is-plain-object.js';
export const resolveSchema = (schemaOrRef, ctx, location) => {
    if (isRef(schemaOrRef)) {
        const resolved = ctx.resolve(schemaOrRef, location?.source.absoluteRef);
        return resolved
            ? { schema: resolved.node, location: resolved.location, chain: resolved.chain }
            : { schema: undefined, location };
    }
    return { schema: schemaOrRef, location };
};
const schemaDefinesProperty = (schema, propertyName, ctx, visited, location) => {
    if (schema.properties && getOwn(schema.properties, propertyName) !== undefined) {
        return true;
    }
    if (schema.allOf?.some((branch) => schemaHasProperty(branch, propertyName, ctx, visited, location))) {
        return true;
    }
    if (isNotEmptyArray(schema.anyOf) &&
        schema.anyOf.every((branch) => schemaHasProperty(branch, propertyName, ctx, new Set(visited), location))) {
        return true;
    }
    if (isNotEmptyArray(schema.oneOf) &&
        schema.oneOf.every((branch) => schemaHasProperty(branch, propertyName, ctx, new Set(visited), location))) {
        return true;
    }
    return false;
};
export const schemaHasProperty = (schemaOrRef, propertyName, ctx, visited = new Set(), resolveLocation) => {
    // a $ref can carry sibling keywords that compose the target, so check them first
    if (isRef(schemaOrRef) &&
        schemaDefinesProperty(schemaOrRef, propertyName, ctx, visited, resolveLocation)) {
        return true;
    }
    const { schema, location, chain } = resolveSchema(schemaOrRef, ctx, resolveLocation);
    if (!schema)
        return false;
    if (!visited.has(schema)) {
        visited.add(schema);
        if (schemaDefinesProperty(schema, propertyName, ctx, visited, location)) {
            return true;
        }
    }
    // composed $refs the resolution chased through contribute their sibling keywords too,
    // even when the chain end was already visited through another branch
    for (const chainHop of chain ?? []) {
        if (!isPlainObject(chainHop.node) || visited.has(chainHop.node)) {
            continue;
        }
        visited.add(chainHop.node);
        if (schemaDefinesProperty(chainHop.node, propertyName, ctx, visited, chainHop.location)) {
            return true;
        }
    }
    return false;
};
export function oasTypeOf(value) {
    if (Array.isArray(value)) {
        return 'array';
    }
    else if (value === null) {
        return 'null';
    }
    else if (Number.isInteger(value)) {
        return 'integer';
    }
    else {
        return typeof value;
    }
}
export function hasSecurityRequirements(node) {
    const security = node?.security;
    return Array.isArray(security) && security.length > 0;
}
export function isAsyncOperationSecured(operation, resolve, resolveFrom) {
    if (hasSecurityRequirements(operation))
        return true;
    if (!Array.isArray(operation?.traits))
        return false;
    for (const trait of operation.traits) {
        const traitNode = isRef(trait) ? resolve(trait, resolveFrom).node : trait;
        if (hasSecurityRequirements(traitNode))
            return true;
    }
    return false;
}
/**
 * Checks if value matches specified JSON schema type
 *
 * @param {*} value - value to check
 * @param {JSONSchemaType} type - JSON Schema type
 * @returns boolean
 */
export function matchesJsonSchemaType(value, type, nullable) {
    if (nullable && value === null) {
        return true;
    }
    switch (type) {
        case 'array':
            return Array.isArray(value);
        case 'object':
            return isPlainObject(value);
        case 'null':
            return value === null;
        case 'integer':
            return Number.isInteger(value);
        default:
            return typeof value === type;
    }
}
export function missingRequiredField(type, field) {
    return `${type} object should contain \`${field}\` field.`;
}
export function missingRequiredOneOfFields(type, fields) {
    return `${type} object should contain one of the fields: ${fields
        .map((field) => `\`${field}\``)
        .join(', ')}.`;
}
export function fieldNonEmpty(type, field) {
    return `${type} object \`${field}\` must be non-empty string.`;
}
export function validateDefinedAndNonEmpty({ fieldName, value, ctx, reference, }) {
    if (!isPlainObject(value)) {
        return;
    }
    if (value[fieldName] === undefined) {
        ctx.report({
            message: missingRequiredField(ctx.type.name, fieldName),
            location: ctx.location.child([fieldName]).key(),
            reference,
        });
    }
    else if (!value[fieldName]) {
        ctx.report({
            message: fieldNonEmpty(ctx.type.name, fieldName),
            location: ctx.location.child([fieldName]).key(),
            reference,
        });
    }
}
export function validateOneOfDefinedAndNonEmpty({ fieldNames, value, ctx, reference, }) {
    if (!isPlainObject(value)) {
        return;
    }
    if (!fieldNames.some((fieldName) => value.hasOwnProperty(fieldName))) {
        ctx.report({
            message: missingRequiredOneOfFields(ctx.type.name, fieldNames),
            location: ctx.location.key(),
            reference,
        });
    }
    for (const fieldName of fieldNames) {
        if (value.hasOwnProperty(fieldName) && !value[fieldName]) {
            ctx.report({
                message: fieldNonEmpty(ctx.type.name, fieldName),
                location: ctx.location.child([fieldName]).key(),
                reference,
            });
        }
    }
}
export function getSuggest(given, variants) {
    if (given === null)
        return variants;
    if (typeof given !== 'string' || !variants.length)
        return [];
    const distances = [];
    for (let i = 0; i < variants.length; i++) {
        const distance = levenshtein(given, variants[i]);
        if (distance < 4) {
            distances.push({ distance, variant: variants[i] });
        }
    }
    distances.sort((a, b) => a.distance - b.distance);
    // if (bestMatch.distance <= 4) return bestMatch.string;
    return distances.map((d) => d.variant);
}
export function getExampleValueToValidate(example) {
    if (!isPlainObject(example))
        return undefined;
    if (isDefined(example.dataValue))
        return { value: example.dataValue, field: 'dataValue' };
    if (isDefined(example.value))
        return { value: example.value, field: 'value' };
    return undefined;
}
export function validateExample({ example, schema, options, reference, }) {
    const { location, ctx, validator, allowAdditionalProperties, ajvContext } = options;
    const { resolve, location: parentLocation, report, specVersion } = ctx;
    try {
        const { valid, errors } = validator.validate(example, schema, {
            schemaLoc: parentLocation.child('schema'),
            instancePath: location.pointer,
            resolve,
            allowAdditionalProperties,
            ajvContext,
            specVersion,
        });
        if (!valid) {
            for (const error of errors) {
                report({
                    message: `Example value must conform to the schema: ${error.message}.`,
                    location: {
                        ...new Location(location.source, error.instancePath),
                        reportOnKey: error.keyword === 'unevaluatedProperties' || error.keyword === 'additionalProperties',
                    },
                    from: parentLocation,
                    suggest: error.suggest,
                    reference,
                });
            }
        }
    }
    catch (e) {
        if (e.message === 'discriminator: requires oneOf or anyOf composite keyword') {
            return;
        }
        report({
            message: `Example validation errored: ${e.message}.`,
            location: parentLocation.child('schema'),
            from: parentLocation,
            reference,
        });
    }
}
const MAX_ENUM_VALUES_IN_MESSAGE = 10;
export function validateSchemaEnumType(schemaEnum, propertyValue, propName, refLocation, { report, location }, documentationLink) {
    if (!schemaEnum) {
        return;
    }
    if (!schemaEnum.includes(propertyValue)) {
        const message = schemaEnum.length > MAX_ENUM_VALUES_IN_MESSAGE
            ? `\`${propName}\` "${propertyValue}" is not a valid value.${documentationLink ? ` See the supported values: ${documentationLink}.` : ''}`
            : `\`${propName}\` can be one of the following only: ${schemaEnum
                .map((type) => `"${type}"`)
                .join(', ')}.`;
        report({
            location,
            message,
            from: refLocation,
            suggest: getSuggest(propertyValue, schemaEnum),
        });
    }
}
export function validateResponseCodes({ responseCodes, codeRange, report, reference, }) {
    const responseCodeRegexp = new RegExp(`^${codeRange[0]}[0-9Xx]{2}$`);
    const containsNeededCode = responseCodes.some((code) => (codeRange === '2XX' && code === 'default') || // It's OK to replace 2xx codes with the default
        responseCodeRegexp.test(code));
    if (!containsNeededCode) {
        report({
            message: `Operation must have at least one \`${codeRange}\` response.`,
            location: { reportOnKey: true },
            reference,
        });
    }
}
export function getTagName(tag, ignoreCase) {
    return ignoreCase ? tag.name.toLowerCase() : tag.name;
}
//# sourceMappingURL=utils.js.map