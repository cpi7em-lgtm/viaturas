import { platform } from "node:os";
export function toUnixPathLike(nodePath) {
    if (platform() === "win32") {
        return nodePath.replace(/\\/g, "/");
    }
    return nodePath;
}
//# sourceMappingURL=path.js.map