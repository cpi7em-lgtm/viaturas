export interface FileWriter {
    write(filePath: string, fileContent: string): Promise<void>;
}
export declare class FileSystemWriter implements FileWriter {
    write(filePath: string, fileContent: string): Promise<void>;
}
