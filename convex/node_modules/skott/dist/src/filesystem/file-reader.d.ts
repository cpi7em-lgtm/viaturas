import { Context } from "effect";
export interface FileReader {
    read: (filename: string) => Promise<string>;
    exists: (filename: string) => Promise<boolean>;
    readSync: (filename: string) => string;
    readdir: (root: string, extensions: string[]) => AsyncGenerator<string>;
    stats: (filename: string) => Promise<number>;
    getCurrentWorkingDir: () => string;
}
export declare const FileReader: Context.Tag<FileReader, FileReader>;
export interface FileSystemConfig {
    cwd: string;
    ignorePatterns: string[];
}
export declare class FileSystemReader implements FileReader {
    private readonly config;
    constructor(config: FileSystemConfig);
    private isFileIgnored;
    read(filename: string): Promise<string>;
    exists(filename: string): Promise<boolean>;
    readSync(filename: string): string;
    stats(filename: string): Promise<number>;
    getCurrentWorkingDir(): string;
    readdir(root: string, providedFileExtensions: string[]): AsyncGenerator<string>;
}
