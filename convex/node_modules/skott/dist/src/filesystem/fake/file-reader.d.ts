import type { FileReader, FileSystemConfig } from "../file-reader.js";
export declare class InMemoryFileReader implements FileReader {
    private readonly config;
    constructor(config?: FileSystemConfig);
    private isFileIgnored;
    read(filename: string): Promise<string>;
    readSync(filename: string): string;
    readdir(root: string, fileExtensions: string[]): AsyncGenerator<string>;
    exists(_filename: string): Promise<boolean>;
    stats(_filename: string): Promise<number>;
    getCurrentWorkingDir(): string;
}
