import watcher from "@parcel/watcher";
export declare const watchModeStatus: {
    watching_for_changes: string;
    changes_detected: string;
};
export interface WatchModeOptions {
    cwd: string;
    ignorePatterns: string[];
    fileExtensions: string[];
    verbose?: boolean;
    onChangesDetected: (doneSubscribersPropagation: () => void) => void;
}
export declare function registerWatchMode({ cwd, ignorePatterns, fileExtensions, verbose, onChangesDetected }: WatchModeOptions): Promise<watcher.AsyncSubscription>;
