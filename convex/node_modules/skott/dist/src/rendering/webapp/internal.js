import { ChildProcess, spawn } from "node:child_process";
import path from "node:path";
import { platform } from "node:process";
import compression from "compression";
import isWsl from "is-wsl";
import kleur from "kleur";
import polka from "polka";
import sirv from "sirv";
import resolveWebAppStaticPath from "skott-webapp";
const supportedPlatforms = ["darwin", "win32", "linux"];
function selectPlatformBinary() {
    let binary;
    switch (platform) {
        case "darwin":
            binary = "open";
            break;
        case "win32":
            binary = "explorer.exe";
            break;
        case "linux":
            binary = "xdg-open";
            break;
        default:
            throw new Error(`Unsupported platform: ${process.platform}. Supported platforms are: ${supportedPlatforms.join(", ")}`);
    }
    return binary;
}
function open(url, callback) {
    try {
        let child;
        if (isWsl) {
            child = spawn("cmd.exe", ["/c", "start", url]);
        }
        else {
            const binary = selectPlatformBinary();
            child = spawn(binary, [url]);
        }
        child.on("error", callback);
    }
    catch (error) {
        callback(error);
    }
}
function findSkottWebAppDirectory() {
    const skottWebAppDirectory = resolveWebAppStaticPath();
    if (!skottWebAppDirectory) {
        throw new Error("package 'skott-webapp' could not be found. Please install it as a dependency.");
    }
    return skottWebAppDirectory;
}
export function createHttpApp(port) {
    const skottWebAppPath = findSkottWebAppDirectory();
    const compress = compression();
    const assets = sirv(skottWebAppPath, {
        immutable: true
    });
    const app = polka().use(compress, assets);
    return {
        app,
        listen: (inputOptions) => {
            app.listen(port);
            // @ts-expect-error - port exists
            const bindedAddress = `http://localhost:${app.server?.address()?.port}`;
            if (inputOptions.onListen) {
                inputOptions.onListen(port);
            }
            else {
                console.log(`\n ${kleur.bold(`💻 Web application is ready:`)} ${kleur
                    .bold()
                    .underline()
                    .magenta(`${bindedAddress}`)}`);
            }
            if (!inputOptions.autoOpen) {
                return;
            }
            open(bindedAddress, (error) => {
                if (error) {
                    if (inputOptions.onOpenError) {
                        inputOptions.onOpenError(error);
                        return;
                    }
                    console.error(`\n ${kleur
                        .red()
                        .bold(`Could not automatically open the application on ${bindedAddress}. Reason: "${error.message ?? "unknown"}"`)}
                    
              \n ${kleur
                        .yellow()
                        .bold("Application remains accessible manually")}
                `);
                }
            });
        }
    };
}
export function resolveEntrypointPath(options) {
    const { entrypoint, includeBaseDir } = options;
    let baseEntrypointPath;
    if (includeBaseDir && entrypoint) {
        baseEntrypointPath = path.join(path.dirname(entrypoint), entrypoint);
    }
    else if (entrypoint) {
        baseEntrypointPath = path.basename(entrypoint);
    }
    return baseEntrypointPath;
}
//# sourceMappingURL=internal.js.map