import type { EventEmitter } from "stream";
interface Reredenrable {
    rerender(): void | Promise<void>;
}
export declare class CliComponent implements Reredenrable {
    private readonly renderFunction;
    constructor(renderFunction: () => void);
    rerender(): void;
}
export declare class RenderManager {
    private readonly eventEmitter;
    private readonly components;
    asyncComponents: Array<() => Promise<void>>;
    constructor(eventEmitter: EventEmitter, components?: Reredenrable[]);
    afterRenderingPhase(done: () => void): Promise<void>;
    renderOnChanges(displayMode: Reredenrable): void;
}
export {};
