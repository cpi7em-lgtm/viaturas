import type { DiGraph } from "digraph-js";
import type { SkottConfig } from "../skott.js";
import type { SkottNode } from "./node.js";
export declare const CollectLevel: {
    readonly Deep: "deep";
    readonly Shallow: "shallow";
};
export type CollectLevelValues = (typeof CollectLevel)[keyof typeof CollectLevel];
export declare class GraphApi<T> {
    private readonly graph;
    private readonly config;
    constructor(graph: DiGraph<SkottNode<T>>, config: SkottConfig<T>);
    getFileNode(id: string): SkottNode<T>;
    getNodes(): Record<string, SkottNode<T>>;
    traverseFiles(options?: {
        rootFile?: string;
        moduleImportsCollection?: "deepFirst" | "shallowFirst";
    }): Generator<SkottNode<T>, void, void>;
    collectFilesDependencies(rootFile: string, collectLevel: CollectLevelValues): SkottNode<T>[];
    collectFilesDependingOn(rootFile: string, collectLevel: CollectLevelValues): SkottNode<T>[];
    collectUnusedFiles(): Array<SkottNode["id"]>;
    hasCircularDependencies(): boolean;
    findCircularDependencies(): Array<Array<SkottNode["id"]>>;
    findLeaves(): Array<SkottNode["id"]>;
}
