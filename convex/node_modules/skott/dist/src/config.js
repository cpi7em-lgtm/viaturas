import { identity, pipe, Option } from "effect";
import * as E from "fp-ts/lib/Either.js";
import * as D from "io-ts/lib/Decoder.js";
import { dependencyResolverDecoder } from "./modules/resolvers/base-resolver.js";
import { defaultConfig } from "./skott.js";
function raiseIllegalConfigException(configuration) {
    throw new Error(`Illegal configuration: ${configuration}`);
}
export function checkIllegalRuntimeConfigs(config) {
    if (Option.isSome(config)) {
        const { entrypoint, includeBaseDir, cwd } = config.value;
        if (!entrypoint && includeBaseDir) {
            raiseIllegalConfigException("`includeBaseDir` can only be used when providing an entrypoint");
        }
        if (entrypoint && cwd && cwd !== process.cwd()) {
            raiseIllegalConfigException("`cwd` can't be customized when providing an entrypoint");
        }
    }
}
function withDefaultValue(defaultValue) {
    return (decoder) => {
        return {
            decode: (v) => v === undefined ? D.success(defaultValue) : decoder.decode(v)
        };
    };
}
const decodeGroupBy = {
    // Typecast is ok here as we can't really runtime-check (before any calls) that the provided function
    // will always return a valid value anyawy
    // Separate runtime checks will be done during the actual usage of the function for each call
    decode: (input) => typeof input === "function"
        ? D.success(input)
        : D.failure(input, "`groupBy` must be a function or not provided at all")
};
const getConfig = () => D.struct({
    entrypoint: withDefaultValue(defaultConfig.entrypoint)(D.string),
    includeBaseDir: withDefaultValue(defaultConfig.includeBaseDir)(D.boolean),
    incremental: withDefaultValue(defaultConfig.incremental)(D.boolean),
    circularMaxDepth: withDefaultValue(defaultConfig.circularMaxDepth)(D.number),
    dependencyTracking: withDefaultValue(defaultConfig.dependencyTracking)(D.struct({
        thirdParty: D.boolean,
        builtin: D.boolean,
        typeOnly: D.boolean
    })),
    fileExtensions: withDefaultValue(defaultConfig.fileExtensions)(D.array(D.literal(".js", ".ts", ".jsx", ".tsx", ".mjs", ".cjs"))),
    tsConfigPath: withDefaultValue(defaultConfig.tsConfigPath)(D.string),
    manifestPath: withDefaultValue(defaultConfig.manifestPath)(D.string),
    dependencyResolvers: withDefaultValue(defaultConfig.dependencyResolvers)(D.array(dependencyResolverDecoder())),
    groupBy: withDefaultValue(defaultConfig.groupBy)(decodeGroupBy),
    /**
     * External runner only config
     */
    cwd: withDefaultValue(process.cwd())(D.string),
    verbose: withDefaultValue(false)(D.boolean),
    ignorePatterns: withDefaultValue([])(D.array(D.string))
});
export function decodeInputConfig(partialConfig) {
    return pipe(partialConfig, Option.getOrNull, getConfig().decode, E.fold((decodeError) => {
        throw new Error(`Invalid skott config. Reason: ${D.draw(decodeError)}`);
    }, identity));
}
//# sourceMappingURL=config.js.map