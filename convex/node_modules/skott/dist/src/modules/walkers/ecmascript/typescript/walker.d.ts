import type { ModuleWalker, ModuleWalkerResult, WalkerOptions } from "../../common.js";
export declare class TypeScriptModuleWalker implements ModuleWalker {
    walk({ fileContent, fileName, config, logger }: WalkerOptions): Promise<ModuleWalkerResult>;
}
