import { builtinModules } from "node:module";
import path from "node:path";
import { Option, Effect } from "effect";
import { FileReader } from "../../../filesystem/file-reader.js";
import { highlight } from "../../../logger.js";
import { isTypeScriptPathAlias, isTypeScriptRelativePathWithNoLeadingIdentifier, resolvePathAlias } from "../../walkers/ecmascript/typescript/path-alias.js";
import { continueResolution, kExpectedModuleExtensions } from "../base-resolver.js";
const NODE_PROTOCOL = "node:";
export function isBuiltinModule(module) {
    // fs, path, etc
    if (builtinModules.includes(module)) {
        return true;
    }
    // node:fs
    if (module.startsWith("node:")) {
        const moduleName = module.slice(NODE_PROTOCOL.length);
        // node:fs/promises
        if (module.includes("/")) {
            return isBuiltinModule(moduleName);
        }
        return builtinModules.includes(moduleName);
    }
    else if (module.includes("/")) {
        const [moduleName] = module.split("/");
        return builtinModules.includes(moduleName);
    }
    return false;
}
export function isThirdPartyModule(module, expectedModuleExtensions, manifests) {
    const extension = path.extname(module);
    const hasExpectedExtension = extension !== "" && expectedModuleExtensions.has(extension);
    const isThirdPartyUsingHeuristics = !module.startsWith(".") && !hasExpectedExtension;
    /**
     * Only single manifest is supported for now. In the case where we have multiple
     * manifest, we must precisely determine which manifest to use to decide
     * if a module should be flagged as third-party as each dependency should be
     * scoped by its parent package.json.
     *
     * In the case where there are multiple manifests, we just want to rely on
     * the source code heuristics.
     */
    if (Object.keys(manifests).length === 1) {
        const rootManifest = manifests[Object.keys(manifests)[0]];
        const rootProductionDeps = Object.keys(rootManifest.dependencies ?? {});
        return (rootProductionDeps.some((dep) => module.startsWith(dep)) ||
            isThirdPartyUsingHeuristics);
    }
    return isThirdPartyUsingHeuristics;
}
export function extractNpmNameFromThirdPartyModuleDeclaration(moduleDeclarationPath) {
    const declarationPathSegments = moduleDeclarationPath.split("/");
    const scopeOrName = declarationPathSegments[0];
    const isScopedPackage = scopeOrName.startsWith("@");
    if (isScopedPackage) {
        return declarationPathSegments.slice(0, 2).join("/");
    }
    return scopeOrName;
}
export function isJSONModule(module) {
    return module.endsWith(".json");
}
export function isBinaryModule(module) {
    return module.endsWith(".node");
}
export function isJavaScriptModule(module) {
    const extension = path.extname(module);
    return (extension === ".js" ||
        extension === ".jsx" ||
        extension === ".mjs" ||
        extension === ".cjs");
}
export function isTypeScriptProject(tsConfigPath) {
    return FileReader.pipe(Effect.flatMap((fileReader) => Effect.tryPromise(() => fileReader.exists(path.join(fileReader.getCurrentWorkingDir(), tsConfigPath)))));
}
export function isTypeScriptModule(module) {
    const extension = path.extname(module);
    return extension === ".ts" || extension === ".tsx";
}
export function isTypeScriptDeclarationFile(module) {
    return module.endsWith(".d.ts");
}
export function isTestFile(fileName) {
    return fileName.includes(".test") || fileName.includes(".spec");
}
export function isMinifiedFile(fileName) {
    return fileName.includes(".min");
}
function collectThirdPartyModule(moduleDeclaration, resolvedNodePath, projectGraph) {
    const dependencyName = extractNpmNameFromThirdPartyModuleDeclaration(moduleDeclaration);
    projectGraph.mergeVertexBody(resolvedNodePath, (body) => {
        body.thirdPartyDependencies = Array.from(new Set(body.thirdPartyDependencies.concat(dependencyName)));
    });
}
function lowlightSkipped(moduleDeclaration) {
    return `Skipped ${highlight(moduleDeclaration)}`;
}
function highlightResolved(moduleDeclaration, category) {
    return `Resolved ${highlight(moduleDeclaration)} as a ${highlight(category)} module`;
}
export class EcmaScriptDependencyResolver {
    async resolve({ moduleDeclaration, projectGraph, config, rawNodePath, resolvedNodePath, workspaceConfiguration, logger, followModuleDeclaration }) {
        if (isBinaryModule(moduleDeclaration) || isJSONModule(moduleDeclaration)) {
            logger.info(lowlightSkipped(moduleDeclaration));
            return continueResolution();
        }
        if (isBuiltinModule(moduleDeclaration)) {
            if (!config.dependencyTracking.builtin) {
                logger.info(lowlightSkipped(moduleDeclaration));
                return continueResolution();
            }
            logger.success(highlightResolved(moduleDeclaration, "builtin"));
            projectGraph.mergeVertexBody(resolvedNodePath, (body) => {
                body.builtinDependencies =
                    body.builtinDependencies.concat(moduleDeclaration);
            });
        }
        else if (isTypeScriptPathAlias(moduleDeclaration, workspaceConfiguration.pathAliases)) {
            const resolvedModulePath = resolvePathAlias(moduleDeclaration, path.dirname(config.tsConfigPath), workspaceConfiguration.pathAliases, logger);
            if (Option.isSome(resolvedModulePath)) {
                logger.success(highlightResolved(moduleDeclaration, "TypeScript path alias"));
                await followModuleDeclaration({
                    moduleDeclaration: resolvedModulePath.value,
                    rootPath: resolvedNodePath,
                    isPathAliasDeclaration: true
                });
            }
            else {
                logger.failure(`${highlight(moduleDeclaration)} alias could not be resolved`);
            }
        }
        else if (isTypeScriptRelativePathWithNoLeadingIdentifier(workspaceConfiguration.typescript.baseUrl, moduleDeclaration)) {
            const moduleSuccessfullyResolved = await followModuleDeclaration({
                rootPath: rawNodePath,
                moduleDeclaration,
                isPathAliasDeclaration: true,
                pathAliasBaseUrl: workspaceConfiguration.typescript.baseUrl
            });
            // In the context of TypeScript, a module declaration such as "libs/foo" could
            // be interpreted as a path alias or a third-party module. If the module
            // is not found following the module resolution, it means that it is probably a third-party
            // module.
            if (!moduleSuccessfullyResolved &&
                isThirdPartyModule(moduleDeclaration, kExpectedModuleExtensions, workspaceConfiguration.manifests)) {
                if (!config.dependencyTracking.thirdParty) {
                    logger.info(lowlightSkipped(moduleDeclaration));
                    return continueResolution();
                }
                logger.success(highlightResolved(moduleDeclaration, "third-party"));
                collectThirdPartyModule(moduleDeclaration, resolvedNodePath, projectGraph);
            }
        }
        else if (isThirdPartyModule(moduleDeclaration, kExpectedModuleExtensions, workspaceConfiguration.manifests)) {
            if (!config.dependencyTracking.thirdParty) {
                logger.info(lowlightSkipped(moduleDeclaration));
                return continueResolution();
            }
            logger.success(highlightResolved(moduleDeclaration, "third-party"));
            collectThirdPartyModule(moduleDeclaration, resolvedNodePath, projectGraph);
        }
        else {
            logger.success(highlightResolved(moduleDeclaration, "file"));
            await followModuleDeclaration({
                rootPath: rawNodePath,
                moduleDeclaration
            });
        }
        return continueResolution();
    }
}
//# sourceMappingURL=resolver.js.map